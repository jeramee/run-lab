# Separation

`run-lab` is the user-facing notebook/research workbench.

It may consume `evidence-ai-core` later, but this rough scaffold keeps an internal bridge so it can run alone.

It must not contain TraceLab lab/instrument action records.
